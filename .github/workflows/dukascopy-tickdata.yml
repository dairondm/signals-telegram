name: Descarga de tick data de Dukascopy

on:
  workflow_dispatch:
    inputs:
      symbol:
        description: "Código del instrumento en Dukascopy"
        required: true
        default: "XAUUSD"
      start_date:
        description: "Fecha de inicio (YYYY-MM-DD, inclusive)"
        required: true
        default: "2026-01-01"
      end_date:
        description: "Fecha de fin (YYYY-MM-DD, inclusive). Deja vacío para usar hoy."
        required: false
        default: ""
      upload_raw_bi5:
        description: "También comprimir y subir a Drive los archivos .bi5 crudos"
        type: boolean
        required: false
        default: true
      reuse_csv_from_run_id:
        description: "OPCIONAL: ID de una corrida anterior de este workflow. Si lo llenas, se salta la descarga/conversión y solo se baja el CSV que esa corrida ya generó (artifact) y se sube a Drive."
        required: false
        default: ""

jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install -r dukascopy/requirements.txt

      - name: Resolver fecha de fin
        id: dates
        run: |
          if [ -z "${{ github.event.inputs.end_date }}" ]; then
            echo "end_date=$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"
          else
            echo "end_date=${{ github.event.inputs.end_date }}" >> "$GITHUB_OUTPUT"
          fi

      # Conserva los archivos crudos entre re-ejecuciones de este workflow
      # para que una corrida fallida o cortada por timeout pueda reanudar
      # en vez de descargar todo de nuevo. La key incluye el run_id para
      # que SIEMPRE se guarde al final (actions/cache no re-guarda en un
      # cache hit de key exacta); restore-keys busca la corrida previa más
      # reciente con las mismas fechas para restaurar su progreso.
      - name: Cachear tick data cruda
        if: ${{ github.event.inputs.reuse_csv_from_run_id == '' }}
        uses: actions/cache@v4
        with:
          path: dukascopy_data/raw
          key: dukascopy-raw-${{ github.event.inputs.symbol }}-${{ github.event.inputs.start_date }}-${{ steps.dates.outputs.end_date }}-${{ github.run_id }}
          restore-keys: |
            dukascopy-raw-${{ github.event.inputs.symbol }}-${{ github.event.inputs.start_date }}-${{ steps.dates.outputs.end_date }}-

      - name: Descargar archivos .bi5 crudos
        if: ${{ github.event.inputs.reuse_csv_from_run_id == '' }}
        run: |
          python dukascopy/download_bi5.py \
            --symbol "${{ github.event.inputs.symbol }}" \
            --start "${{ github.event.inputs.start_date }}" \
            --end "${{ steps.dates.outputs.end_date }}" \
            --out-dir dukascopy_data/raw \
            --concurrency 6 \
            --rate-limit 4

      # No aborta el job aunque falten horas (download_bi5.py solo sale
      # con error si no se logró NINGÚN dato); esto solo deja constancia
      # visible de qué faltó, para saber si conviene volver a correr el
      # workflow y completar el rango.
      - name: Reportar horas faltantes
        if: ${{ always() && github.event.inputs.reuse_csv_from_run_id == '' }}
        run: |
          F="dukascopy_data/raw/_failed_hours_${{ github.event.inputs.symbol }}.txt"
          if [ -f "$F" ]; then
            COUNT=$(wc -l < "$F")
            echo "### ⚠️ $COUNT horas no se pudieron descargar" >> "$GITHUB_STEP_SUMMARY"
            echo "Vuelve a ejecutar este workflow con las mismas fechas para reintentarlas (lo ya descargado se conserva en caché)." >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            head -n 50 "$F" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          else
            echo "### ✅ Todas las horas del rango se descargaron correctamente" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Convertir a CSV
        if: ${{ github.event.inputs.reuse_csv_from_run_id == '' }}
        run: |
          python dukascopy/convert_to_csv.py \
            --symbol "${{ github.event.inputs.symbol }}" \
            --in-dir dukascopy_data/raw \
            --out-csv dukascopy_data/csv/${{ github.event.inputs.symbol }}_${{ github.event.inputs.start_date }}_${{ steps.dates.outputs.end_date }}.csv

      - name: Comprimir archivos .bi5 crudos
        if: ${{ github.event.inputs.upload_raw_bi5 == 'true' && github.event.inputs.reuse_csv_from_run_id == '' }}
        run: |
          zip -r -q dukascopy_data/${{ github.event.inputs.symbol }}_${{ github.event.inputs.start_date }}_${{ steps.dates.outputs.end_date }}_raw.zip \
            dukascopy_data/raw/${{ github.event.inputs.symbol }}

      # Cuando reuse_csv_from_run_id está lleno, no se descarga nada de
      # Dukascopy: se toma directamente el CSV que esa corrida anterior ya
      # generó y guardó como artifact, para no repetir 40+ minutos de
      # descarga solo para reintentar la subida a Drive.
      - name: Descargar CSV de una corrida anterior
        if: ${{ github.event.inputs.reuse_csv_from_run_id != '' }}
        uses: actions/download-artifact@v4
        with:
          name: ${{ github.event.inputs.symbol }}-tickdata-csv
          run-id: ${{ github.event.inputs.reuse_csv_from_run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: dukascopy_data/csv

      - name: Subir CSV a Google Drive
        env:
          GDRIVE_OAUTH_CLIENT_ID: ${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
          GDRIVE_OAUTH_CLIENT_SECRET: ${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
          GDRIVE_OAUTH_REFRESH_TOKEN: ${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}
          GDRIVE_SA_JSON: ${{ secrets.GDRIVE_SA_JSON }}
        run: |
          FILE=$(find dukascopy_data/csv -name '*.csv' | head -n 1)
          if [ -z "$FILE" ]; then
            echo "No se encontró ningún .csv en dukascopy_data/csv" >&2
            exit 1
          fi
          python dukascopy/upload_to_drive.py \
            --file "$FILE" \
            --folder-id "${{ secrets.GDRIVE_FOLDER_ID }}"

      - name: Subir zip de .bi5 crudos a Google Drive
        if: ${{ github.event.inputs.upload_raw_bi5 == 'true' && github.event.inputs.reuse_csv_from_run_id == '' }}
        env:
          GDRIVE_OAUTH_CLIENT_ID: ${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
          GDRIVE_OAUTH_CLIENT_SECRET: ${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
          GDRIVE_OAUTH_REFRESH_TOKEN: ${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}
          GDRIVE_SA_JSON: ${{ secrets.GDRIVE_SA_JSON }}
        run: |
          python dukascopy/upload_to_drive.py \
            --file dukascopy_data/${{ github.event.inputs.symbol }}_${{ github.event.inputs.start_date }}_${{ steps.dates.outputs.end_date }}_raw.zip \
            --folder-id "${{ secrets.GDRIVE_FOLDER_ID }}"

      # Respaldo para que los datos sigan siendo recuperables aunque la
      # subida a Drive o los secrets aún no estén configurados. Los
      # artifacts expiran a los 90 días por defecto.
      - name: Subir CSV como artifact del workflow (respaldo)
        if: ${{ always() && github.event.inputs.reuse_csv_from_run_id == '' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.symbol }}-tickdata-csv
          path: dukascopy_data/csv/*.csv
          retention-days: 30
